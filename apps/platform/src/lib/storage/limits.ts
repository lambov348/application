/**
 * Предел на размер файла отдельным модулем — его читает и сервер, и форма
 * в телефоне. В @/lib/storage его брать нельзя: тот модуль тянет драйверы,
 * а вместе с ними в браузерную сборку попал бы серверный код.
 */
export const MAX_UPLOAD_BYTES = 12 * 1024 * 1024;
