# Без этого в контекст сборки уезжает больше гигабайта: зависимости и
# прошлая сборка ставятся внутри образа заново.
node_modules
.next
storage
.git

# Секреты в образ не попадают никогда — они приходят через окружение.
.env
.env.*
!.env.example

# Служебное
*.local.mjs
*.local.ts
npm-debug.log*
.DS_Store
